import { StyleSheet } from "react-native";
import { SCREEN_HEIGHT, scaleByHeight, scaleByWidth } from "src/assets/helpers";

export const styles = StyleSheet.create({
  container: {
    padding: scaleByWidth(20),
  },
});
